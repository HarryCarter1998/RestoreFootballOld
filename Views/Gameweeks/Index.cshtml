@model IEnumerable<RestoreFootball.Models.Gameweek>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.GreenScore)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NonBibsScore)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.YellowScore)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OrangeScore)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.GreenScore)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NonBibsScore)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.YellowScore)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OrangeScore)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
